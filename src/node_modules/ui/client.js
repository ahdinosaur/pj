var createReactStream = require('react-render-stream')
var LedGridUi = require('ledgrid-ui')

module.exports = createUi

function createUi (config) {
  return createReactStream(
    LedGridUi,
    document.body.querySelector(config.element)
  )
}  
