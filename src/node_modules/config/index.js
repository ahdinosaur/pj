var Path = require('path')
var assign = require('lodash.assign')

var configDir = '../../../config/'
var env = require('util/env')

module.exports = assign(
  {},
  require(Path.join(configDir, env)),
  require(Path.join(configDir, 'defaults'),
  { env: env },
)
